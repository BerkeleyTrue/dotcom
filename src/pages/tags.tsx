import * as _ from 'lodash/fp';
import { Text } from '@chakra-ui/react';
import { Layout } from '../components/Layout';

const Tags = () => {
  return (
    <Layout>
      <Text>{'<==tags==>'}</Text>
    </Layout>
  );
};

export default Tags;
